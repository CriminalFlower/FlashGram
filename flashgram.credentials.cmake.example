# FlashGram local Telegram API credentials.
#
# Do NOT fill this example file. Run flashgram_setup_credentials.ps1, which
# writes flashgram.credentials.cmake next to it. That file is ignored by git.
set(TDESKTOP_API_ID 0)
set(TDESKTOP_API_HASH "")
